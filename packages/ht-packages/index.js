import { validateConfig, mergeConfigs, getPackageInfo } from '@holithemes/shared-utils';

export { validateConfig, mergeConfigs, getPackageInfo } from '@holithemes/shared-utils';

export default {
  validateConfig,
  mergeConfigs,
  getPackageInfo
};